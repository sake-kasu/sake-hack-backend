BadRequest:
  description: リクエスト不正
  content:
    application/json:
      schema:
        $ref: "../schemas/common.yaml#/ErrorResponse"

NotFound:
  description: リソースが見つからない
  content:
    application/json:
      schema:
        $ref: "../schemas/common.yaml#/ErrorResponse"

InternalServerError:
  description: サーバー内部エラー
  content:
    application/json:
      schema:
        $ref: "../schemas/common.yaml#/ErrorResponse"

Unauthorized:
  description: 認証失敗
  content:
    application/json:
      schema:
        $ref: "../schemas/common.yaml#/ErrorResponse"

Forbidden:
  description: 権限エラー
  content:
    application/json:
      schema:
        $ref: "../schemas/common.yaml#/ErrorResponse"


Conflict:
  description: リソースの競合エラー
  content:
    application/json:
      schema:
        $ref: "../schemas/common.yaml#/ErrorResponse"
