openapi: 3.0.3
info:
  title: Sake Hack Backend API
  version: 1.0.0
  description: |
    Sake Hack Backend API
  contact:
    name: Sake Hack Team
servers:
  - url: http://localhost:8080
    description: Local development server

security: []  # 現時点では認証なし（Phase 3で実装予定）

paths:
  /sakes:
    $ref: "./paths/sakes.yaml"
  /sakes/{id}:
    $ref: "./paths/sakeDetail.yaml"
  /stocks:
    $ref: "./paths/stocks.yaml"
  /stocks/{id}:
    $ref: "./paths/stockDetail.yaml"

tags:
  - name: sakes
    description: 酒関連API（公開閲覧用）
  - name: stocks
    description: 在庫管理API（店舗用）
